import InjectionToken from '../injection-token.js';

/**
 * Token to hold the parent container
 *
 */
export const ParentContainerToken = new InjectionToken('__PARENT__');

export default ParentContainerToken;
